(function(window) {
    window["env"] = window["env"] || {};
  
    // Environment variables
    window["env"]["pageHeader"] = "${PAGE_HEADER}";
    window["env"]["pageHeaderIcon"] = "${PAGE_HEADER_ICON}";
    window["env"]["dataSource"] = "${DATA_SOURCE}";
    window["env"]["debug"] = "${DEBUG}";
})(this);